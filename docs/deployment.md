# 🚀 Deployment Guide

## Requirements

- Java 21
- Maven
- MySQL 8+

## Steps

1. Clone the repository.
2. Create the `finflow` database.
3. Configure `application.properties`.
4. Run:

```bash
mvn spring-boot:run
```

The application will be available at:

```
http://localhost:8080
```